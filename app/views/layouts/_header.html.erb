<nav>
    <div class="">
      <div class="flex justify-between h-16 px-10 shadow items-center">
        <div class="flex items-center space-x-8">
          <h1 class="text-xl lg:text-2xl font-bold cursor-pointer">Mosec</h1>
          <div class="flex hidden md:flex space-x-4">
            <%= link_to "Home", root_path, class:"hover:text-indigo-600 text-gray-700" %>
            <%= link_to "About", about_path,  class:"hover:text-indigo-600 text-gray-700"%>
            <%= link_to "Services", services_path, class:"hover:text-indigo-600 text-gray-700"%>
            <a href="#" class="hover:text-indigo-600 text-gray-700">Contact</a>
          </div>
        </div>
        <div class="flex space-x-4 items-center">
          <% if user_signed_in? %>
            <%= button_to "SIGNOUT", destroy_user_session_path, method: :delete, class:"bg-indigo-600 px-4 py-2 rounded text-white hover:bg-indigo-500 text-sm" %>
          <% else %>
            <%= link_to "LOGIN", new_user_session_path, class:"text-gray-800 text-sm" %>
            <%= link_to "SIGNUP", new_user_registration_path, class:"bg-indigo-600 px-4 py-2 rounded text-white hover:bg-indigo-500 text-sm" %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
